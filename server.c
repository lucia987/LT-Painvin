#include <sys/socket.h>
#include <netinet/in.h>
#include <arpa/inet.h>
#include <netdb.h>

int main()
{
	/* Create socket */

	/* Bind socket */

	/* Listen on socket */

	/* Accept incoming connection */

	/* Receive message */

	/* Send confirmation */

	/* Close socket */
	return 0;
}
